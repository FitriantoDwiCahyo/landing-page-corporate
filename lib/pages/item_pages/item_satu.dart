import 'package:flutter/material.dart';

class ItemSatu extends StatelessWidget {
  const ItemSatu({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}